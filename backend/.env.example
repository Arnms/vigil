# ===========================================
# Vigil API Monitoring - Environment Variables
# ===========================================

# -------------------------------------------
# Application Settings
# -------------------------------------------
NODE_ENV=development
PORT=3000

# -------------------------------------------
# Database Configuration (PostgreSQL)
# -------------------------------------------
# For local development (Docker)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=api_monitor

# For production (Render.com)
# DATABASE_URL=postgresql://user:password@host:port/database
# DATABASE_HOST=your-db.oregon-postgres.render.com
# DATABASE_PORT=5432
# DATABASE_USER=vigil_user
# DATABASE_PASSWORD=your-password
# DATABASE_NAME=vigil

# -------------------------------------------
# Redis Configuration
# -------------------------------------------
# For local development (Docker)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# For production (Upstash Redis)
# REDIS_HOST=your-id.upstash.io
# REDIS_PORT=6379
# REDIS_PASSWORD=your-upstash-token
# REDIS_TLS=true
# REDIS_DB=0

# -------------------------------------------
# CORS Configuration
# -------------------------------------------
# For local development
CORS_ORIGIN=http://localhost:5173

# For production (Render.com)
# CORS_ORIGIN=https://vigil-frontend-demo.onrender.com

# -------------------------------------------
# Email Notification (SMTP)
# -------------------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@vigil.com

# -------------------------------------------
# Slack Notification (Optional)
# -------------------------------------------
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# -------------------------------------------
# Health Check Settings
# -------------------------------------------
# Default check interval in seconds (60 = 1 minute)
DEFAULT_CHECK_INTERVAL=60

# Default timeout threshold in milliseconds
DEFAULT_TIMEOUT_THRESHOLD=5000

# -------------------------------------------
# Production Notes
# -------------------------------------------
# 1. Never commit .env file to version control
# 2. Use strong passwords in production
# 3. Enable TLS for Redis in production (REDIS_TLS=true)
# 4. Set synchronize=false in TypeORM for production
# 5. Configure proper CORS_ORIGIN for your frontend domain
